import { Routes } from '@angular/router';
import { AddPatientComponent } from './add-patient/add-patient.component';

export const routes: Routes = [
  { path: 'post', component: AddPatientComponent }
];
